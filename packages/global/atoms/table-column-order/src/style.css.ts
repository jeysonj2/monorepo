import { css } from 'lit';

export const style = css`
  :host {
    --list-item-padding: var(--tablecolumnorder-list-item-padding, 0.5rem);
    --card-width: var(--tablecolumnorder-card-width, 512px);
  }
`;
