import { css } from 'lit';

export const style = css`
  iz-menu {
    /** TODO this is not necessary ? */
    --menu-button-width: 3rem;
  }

  .menu-item-flex {
    display: flex;
    justify-content: space-around;
    align-items: center;
    width: 60%;
  }
`;
