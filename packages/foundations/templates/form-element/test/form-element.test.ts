import { expect } from '@open-wc/testing';
import '../src/form-element.js';

describe('FormElement', () => {
  it.skip('should find closest form', async () => {
    expect(1).to.be.equal(1);
  });
});
