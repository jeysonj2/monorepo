import { css } from 'lit';

export const style = css`
  :host {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
  }

  iz-typography {
    --typography-display: inline;
  }
`;
